#!/bin/bash

# 运维管理中 "重启" 将运行此脚本
#            "发布&重启" 将先运行"build"脚本，再运行此脚本


# 重启服务
# 需考虑: 1.服务未启动时，进行启动   # 如 nginx
#         2.服务已启动时，进行重启   # 如 nginx -s reload
# ****启动服务后脚本必须退出: cmd >run.log 2>&1 &

# exit 1  # 脚本运行失败
# 脚本运行成功退出码为0，非0视为失败
set -x

sh bin/start
